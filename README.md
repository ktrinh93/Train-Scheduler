# Train-Scheduler

This app uses Firebase as its real-time database used to store and keep track of train information.
The user is able to enter inputs describing the details of a train. Upon submission, the train information will be saved to the database. When new information is stored in the database, the page will automatically update with that information.
The user is also able to delete trains.
